const initialState = {
    isLoading: false, 
    pokemonList: [],
    error: ''
}

export const reducer = (state = initialState, action) => {
    switch(action.type){
        default: 
            return state
    }
}